import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "MediNote AI – Ambient Clinical Note Generator",
  description: "AI-powered clinical documentation that listens to doctor-patient conversations and automatically generates structured clinical notes.",
  keywords: ["healthcare", "AI", "clinical notes", "SOAP", "medical", "transcription"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
